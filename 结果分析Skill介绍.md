# 光伏功率预测结果分析 Skill 介绍

## 1. 技能定位

本技能（`pv-result-analysis`）面向**已完成推理的光伏功率预测结果**，负责从原始预测文件出发，完成指标计算、误差可视化与归因诊断，最终输出可供汇报与决策使用的月度分析报告。它不涉及模型训练，专注于"预测结果评估"这一环节。

---

## 2. 输入要求

### 2.1 必须提供的文件（三个）

| 文件 | 说明 |
|------|------|
| `predicted.parquet` | 模型对未来 48 小时（192 个点，每点 15 分钟）的预测功率值，每个待评估模型（M1-M4 及 ensemble）各一份 |
| `true_label.parquet` | 对应时间段的真实功率（即测试集标签），与预测文件行列对齐 |
| `metric.py` | 项目统一的指标计算脚本，内置五种考核口径的取点与聚合逻辑 |

### 2.2 可选但有价值的输入

| 文件/信息 | 用途 |
|----------|------|
| `训练集 parquet`（2024 全年） | 用于对比 2025 测试期的分布漂移，回答"模型是否遇到训练时没见过的天气" |
| `references/event-log.md` | 记录运维事件（模型改版、气象源切换、限电、扩容），跨月归因时必查 |
| `references/models.md` | M1-M4 及 ensemble 的架构与特征信息，用于解释模型间能力差异 |
| `references/station.md` | 电站位置、容量、限电规则及考核细则 |
| `references/seasonality.md` | 两站气候特征与逐月精度影响机制，归因时的先验知识来源 |

### 2.3 数据范围说明

- 本技能支持两个电站：**雅砻江（柯拉）**与**白马湖**。两站气候机制差异显著，**所有计算与结论均分站独立进行，不混算**。
- 数据粒度：15 分钟，每行为一个滚动窗口样本，含 672 个历史点与 192 个预测点。

---

## 3. 分析策略（三步流程）

### Step 1：数据定位与质检

分析正式开始前，先确认三个关键路径（`metric.py`、真实标签、预测值），并确认数据属于哪个电站。

随后对数据做**系统性质量检查**，包括：
- 预测文件与标签文件的行数一致性、时间戳对齐情况
- 重复时间戳、缺测时段（数量、最长连续缺口）
- 可疑数据模式扫描：夜间功率异常、白天功率异常平顶（疑似限电）、日照时段整段为零（疑似停机）

质检不通过不进行后续计算，可疑日期输出为清单供人工核对。这一步是保证所有下游结论可信度的前提。

---

### Step 2：调用 metric.py，生成月度指标 Excel

调用统一指标脚本，对 M1、M2、M3、M4、ensemble 共五个模型分别计算，产出 **5 个 Excel 文件**，每个文件按月展示三类指标（RMSE、MAE、ACC）。

指标计算覆盖**五种考核口径**，对应业务上对预测精度的不同考核维度：

| 口径 | 含义 |
|------|------|
| `ods_ultra_short` | 超短期考核（4 小时后），先算每天指标再月均 |
| `ultra_short` | 超短期考核（4 小时后），月内所有点合并计算 |
| `ods_short` | 短期/日前考核（次日全天），先算每天指标再月均 |
| `short` | 短期/日前考核（次日全天），月内所有点合并计算 |
| `48hours` | 完整 48 小时预测整体精度 |

`ods_` 与无前缀口径的数值差异是正常现象（前者每天权重相等，后者误差大的天权重更高），出现差异时本身即是分析线索。

---

### Step 3：可视化分析与归因诊断

metric.py 的 Excel 只能告诉"哪个月、哪个口径变差了"，无法回答"为什么"。Step 3 回到逐样本误差做深度分析，分两个层次：

#### 3.1 图谱分析（9 类图）

按需选用，月度对比诊断时 #1/#2/#4/#8 为必画项：

| 图号 | 图名 | 回答的核心问题 |
|------|------|----------------|
| #1 | True vs Pred 归因散点 | 模型是否存在系统性高估或低估？大功率段斜率是否被压低？ |
| #2 | 模型间误差相关性热力图 | 各模型同质化程度——相关系数高说明 ensemble 的组合增益有限 |
| #3 | 月度指标对比折线/条形图 | 哪个月、哪个口径、哪个模型变差 |
| #4 | 逐样本 RMSE 时间序列 | 坏天/坏时段精确定位，模型间差距出现的具体日期 |
| #5 | 按预报时效的误差曲线 | 误差随预测时效增长的形态；超短期与短期口径差异的来源 |
| #6 | 日内时段误差剖面 | 早晚坡段 vs 正午谁更差；高估/低估的日内不对称性 |
| #7 | 坏天日历热力图 | 坏天是否集中连片（天气过程）还是分散（系统性退化） |
| #8 | 天气分型条件对比图 | 模型能力本质对比——差距来自晴天还是多云波动天 |
| #9 | Ensemble Oracle 差距图 | Ensemble 还有多少改进空间；动态选模型是否值得做 |

每张图生成后必须回看，并附文字结论——只发图不给结论视为未完成分析。

#### 3.2 归因诊断（两个高频 Playbook）

**Playbook A："为什么 X 月变差了？"**

按固定流程逐步排查：先查事件记录排除外因 → 图#3 定位口径 → 图#4/#7 定位坏天 → 图#8 分解天气占比变化与类内能力变化 → 检查分布漂移 → 最终给出"天气占比变化 / 分布外运行 / NWP 原料变差"三类候选解释的证据。

**Playbook B："为什么模型 A 比 B 好（差）？"**

先看同质化程度 → 按天气类型分组对比 → 定位差距集中的日期与时段 → 按预报时效分析 → 结合模型档案验证机制解释。

所有"A 比 B 好"类结论须通过**稳健性门槛**（统计显著性检验 + 剔除最差 3 天后结论方向不变），否则表述为"差异不显著"。

#### 3.3 分布漂移诊断（选用）

将 2025 测试期与 2024 训练期的同月数据对比，检测三类漂移：

- **特征漂移**：天气模式（辐照、气温、云波动性）是否偏出训练分布
- **标签漂移**：功率分布是否发生台阶式突变（扩容/限电信号）
- **映射漂移**：功率-辐照物理关系曲线是否整体位移（组件衰减/硬件变化）

---

## 4. 输出内容

### 4.1 结构化数据产物

| 文件 | 内容 |
|------|------|
| `analysis_config.json` | 本次分析的路径配置（电站、metric.py、数据文件路径） |
| `suspect_days.csv` | 数据质检发现的可疑日期清单 |
| `weather_class.csv` | 每日天气分型结果（晴稳 / 阴稳 / 多云波动 / 突变日） |
| M1-M4 + ensemble 共 5 个 Excel | 月度指标表（5 种口径 × RMSE/MAE/ACC） |

### 4.2 图谱文件

所有图输出至 `figures/<电站>/<月份或区间>/`，两站物理隔离。每张图同步产出：
- `.png` 文件（供人查看与报告使用）
- `.stats.json` 文件（包含 R²、相关系数、PSI、KS 检验等精确数值，用于引用）

### 4.3 文字分析文档

| 文档 | 内容 |
|------|------|
| `figures/<电站>/<范围>/ANALYSIS.md` | 每张图的结论（图片引用 + 文字解读），供后续会话接续分析 |
| `FINDINGS.md`（分析工作目录根下） | 顶层结论台账——每条结论含证据链接、状态（假设/已证实/被推翻）、日期，每次分析同步更新 |

---

## 5. 技能边界与注意事项

- **不负责模型训练**，只评估已有预测结果。
- **两站必须分开分析**，任何混算都会因气候机制差异导致结论失真。
- **结论引用必须有数字支撑**（来自 stats.json），references/ 中空字段视为未知信息，不编造。
- **数据异常区分两类**：错误（传感器故障等 → 剔除后计算）与事件（限电/停机等 → 保留数据、记入事件日志、归因时显式考虑）。二者处理方式相反，不可混淆。
- **分组样本不足时不下定论**：单月内某类天气不足 10 天，只描述趋势，不写确定性结论。

---

## 6. 更新说明（本次新增功能）

> 本节说明相对上述基础版本（单一分析技能 + 三步流程）的三项主要升级。三项升级的共同目标是：**让分析结论更可信、可追溯，让人力只花在真正关心的问题上，并让分析可以跨会话、跨机器接续进行。**

### 更新一：分析流程由"三步"重组为"四阶段"，事实与归因物理隔离

**原状况**：原流程为 Step 1（质检）→ Step 2（指标）→ Step 3（可视化与归因）一口气跑完。问题在于"看到现象"与"解释原因"混在同一步，容易出现**先有结论、再挑证据**的事后编故事，且会在用户并不关心的现象上浪费大量归因笔墨。

**新增功能**：将流程明确拆分为四个阶段，并在阶段之间设置纪律边界：

| 阶段 | 内容 | 关键纪律 |
|------|------|----------|
| Stage 1 | 数据与指标（原 Step 1+2） | 质检不通过不继续 |
| Stage 2 | 相关性分析（误差矩阵 + 模型间相关热力图） | 判断模型同质化 |
| Stage 3 | **事实提取**（逐样本误差、坏天定位、天气分型对比） | **只允许写"看到了什么"——现象 + 数字 + 稳健性结果，禁止使用机制/原因语言** |
| Stage 4 | **深度归因**（Playbook + 背景知识 + 假设验证） | **只有到这一步才允许回答"为什么"** |

- **事实与解释物理隔开**：Stage 3 只客观陈述现象，Stage 4 才结合电站背景与模型设计解释原因，从流程上杜绝"为了圆一个结论而挑数据"。
- **Stage 3 → Stage 4 之间是唯一强制停顿点**：先把现象清单汇报给用户，由用户**点名**需要深挖的项，再进入最昂贵的归因分析。这样保证人力和算力只投在真正关心的问题上。
- **阶段进度靠产物自动判定**（指标 Excel 在 = Stage 1 完成；相关性图在 = Stage 2 完成……），无需额外维护状态文件。

### 更新二：结论可信度"三件套"，让每条结论可预注册、可反驳、可追溯

为了让"A 模型比 B 模型好""X 月变差是因为某原因"这类结论经得起推敲，新增三项机制：

1. **假设登记表（`references/hypotheses.md`）**：把由模型机制推导出的可检验假设**预先登记**（如"某损失函数会导致波动天压峰低估"），并写清"如果成立应观察到什么"。要求**先写预测、后看图**，最终结论必须引用对应假设编号——从源头防止事后编故事。
2. **反驳门**：任何结论在被标记为"已证实"之前，必须**逐条排除七类替代解释**（运维事件、样本量不足、天气占比变化、分布漂移、气象原料变差、共享上游模块故障、指标口径错误）。未能排除的，一律降级为"假设"而非"结论"。
3. **运行后回顾 + 变更日志（`CHANGELOG.md`）**：每次实跑后把反馈定向写回技能文件，并逐条记录改动。**改动全程可追溯，改错了能回滚**，避免技能在多次修改中悄然退化。

### 更新三：从单一技能扩展为"双技能协同"的技能包

原来只有一个分析技能，现扩展为两个配套技能，覆盖分析的完整生命周期：

| 技能 | 定位 | 解决的问题 |
|------|------|-----------|
| `pv-result-analysis` | 主分析技能（原有） | 指标计算、可视化、归因诊断；**续跑能力已并入本技能**——每次进入先跑 `scripts/run_orient.py` 定位阶段、跳过已完成步骤、`--goto N` 直达，跨会话/跨机器稳定接续 |
| `pv-model-verify` | **模型档案代码核验**（新增） | 归因依赖"模型看得见/看不见什么"，而这些信息由口述整理、可能记错。本技能对照**真实代码仓库**逐条核验模型档案，以代码为准修正错误、补齐待确认项，保证归因的前提可靠 |

> 注：原独立的 `pv-analysis-resume`（续跑入口）已于 2026-07-10 合并进 `pv-result-analysis` 的 Step 0 Orient，2026-07-13 删除该目录，触发词一并并入主技能 description。

**两者关系**：`pv-model-verify` 保证分析的输入前提（模型档案）正确，`pv-result-analysis` 执行分析主流程（含自带续跑）。两个技能通过统一的 `references/` 知识库和产物约定协同工作。
