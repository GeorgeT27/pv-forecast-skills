# CHANGELOG —— ts-diagnose

<!-- 每行：日期 | 改了哪个文件的哪节 | 触发这次改动的反馈原文（一句话） | 为什么这么改 -->

- 2026-09-09 | 解释环轮次隔离（用户排序的第 6 件）：**没按原计划给产物路径加 `{round}` 占位**——那要动 model-comparison 与 architecture-attribution 两本剧本的全部产物路径，外加 conclusion_gate / build_index / 13 张卡片 / 几十个测试的引用。改成归档式：新脚本 `scripts/new_round.py` 把上一轮的取证与判定产物（`chart_plan.json`/`charts/`/`INDEX.md`/`slice_zcheck.json`/`slice_metrics.csv`/`intervention_plan.json`/`verdict_summary.json`）**移**进 `rounds/round_<N>/`、`state.round` +1、给账本里没标轮次的假设补 `round` 字段、写 `rounds/manifest.json`（记这一轮归了什么、收成几何、未解释哪些、换的什么角度）。产物一走，orient 自然判阶段未完成，入口就回来了——一个路径都不用改。**三样明确不归档**：账本（跨轮累积，新一轮得看得见上一轮否掉了什么）、`receipts/`（按假设 id 命名不撞，且结论闸要求盘上每张都进证据清单，归档掉等于把否证藏起来）、`harvest.json`（本就是追加）。拒绝的情形四条：没跑 harvest_check（不知道解释了多少就换角度=瞎换）、没有 state、没有可归档产物、归档目录已存在（不覆盖），且拒绝时不动盘；`--dry-run` 先看要动什么。**账本混轮**另修：假设条目加 `round` 字段（validate 查 ≥1 整数），`hypothesis_ledger.py` 新增 `--round`/`--receipt-dir`，校验通过且本轮有假设时盖 `gate_reports/ledger_round_<N>.json`（含本轮假设 id 列表 + 账本 sha），本轮一条新假设都没有直接 exit 1；两本剧本的 Stage 1/2 产物挂上这张按轮的章——否则账本文件一直在盘上，Stage 1 永远判「已完成」，新一轮不会被要求登记新假设。`harvest_check` 的轮次改回读 `state.round`（现在 new_round 在维护它了）。conclusion_gate 证据清单必列项加 `rounds/round_*/verdict_summary.json`（防跨轮摘樱桃） | 用户："按照你的顺序来" | ①「换角度再取一轮」此前是条死路：产物全在盘上，orient 判每阶段都 done，直接把人送到末尾；硬做又会盖掉上一轮的取证记录，而那份记录正是判断「第二轮是真换了角度还是换了说法」的依据 | 验证：全包 770 绿 + 17 skip（新增 test_new_round.py 13 例）；真实演练——拿收成为 `none` 的 Weather 工作目录跑完整第二轮：dry-run → 归档 4 件 → orient 的 Stage 0/1 双双重开 → 账本校验因「第 2 轮还没登记任何假设」exit 1 → 追加一条按波动强度分层的 H4(round 2) → 盖章通过 → Stage 1 关闭、Stage 0 待做；H3(round 1, undecided) 全程留在账本里可见
- 2026-09-09 | 收口验收后的五条修复（用户点名按 R2-3 → 软停顿 → R2-6 → R2-5 → R2-4 顺序）：**R2-3 生产者被指去写结论**——orient 在全部阶段完成时无条件打印「可写/刷新 CONCLUSION.md」，5 个不产结论的剧本（data-setup/metric-eval/model-audit/model-comparison/fact-scan）也照收，等于在教 agent 干一件 Stop 钩子拦着的事；改为按 `ec.produces_conclusion(fm)` 分支，生产者改印「交付产物 X 供下游消费，**不写 CONCLUSION.md**」（新增 `_delivers()` 取 produces.id 或末阶段产物名）。**软停顿盘点与补闸**——把 13 个 `pause_after` 逐个对了下一阶段的前置：5 个已有硬闸、2 个是末阶段（无步可跳）、1 个剧本明写允许不停（model-comparison S1「用户不点名则按 orient 推荐推进」，列入豁免）、model-improve S3 其实由 `json:champion.json:converged` 守着（此前误判为软）；真缺口 4 个 —— deployment-drift S2→S3、feature-importance S3→S4、result-eval S3→S4、robustness S3→S4，各补一个 `<domain>-focus` 问题 + 前置（问的就是各自正文早写着的「请用户点名要深挖哪条」），正文同步指向问题 id。**result-eval 那条尤其糟**：前置描述写「用户已点名待深挖现象」而 check 只是 `stage:3`，说的和查的不是一回事。新增守卫测试钉住「每个后面还有阶段的 pause_after，其下一阶段必须有 question:/json: 前置」+ 豁免名单不许留僵尸条目。**R2-6 账本不校验 slice_map**——根因比记的更深：**slice_map 每行的 schema 全引擎从来没写过**，于是真跑产出 `{slice, claimed_by, ...}`、老测试夹具停在 `{dim, bucket, ...}`，而下游（收口脚本、`guard_slices` 默认取值）全都键 `slice`/`claimed_by`，键不上就静默当成没认领；现在 `SLICE_ROW_REQUIRED=["slice","claimed_by"]` 写进 model-comparison 正文并由 `validate_slice_map` 机检（缺字段/重复登记/claimed_by 非 list/同一切片既认领又进 uncovered/内嵌 zcheck 副本与权威产物不符），且 `hypothesis_ledger.py` 新增 `--zcheck`（默认读工作目录 slice_zcheck.json，存在即做认领覆盖对账，不存在则跳过——model-comparison 的切片版图产在别处属正常），夹具一并升级到规格形态。**R2-5 null 当阴性**——`chart_worst_slice_compare` 加结构化 `perm_status`（significant / not-significant / **not-computed**）与 `perm_skip_reason`，剧本汇报条款从两态改三态、`upgrade_rule` 从读 `perm.verdict` 改读 `perm_status` 并写明 not-computed 时该证据线整条缺席（既不计入也不当反证）。**R2-4 文案写死口径**——比原先记的多 4 张：除 worst-slice-compare/rolling-stability/local-waterfall/feature-trend-overlay 外，cross-dim-stability（`row-RMSE mean` 柱标）、feature-regime-error（Y 轴）、model-error-correlation（标题）、oracle-gap（Y 轴）也写死，其中 cross-dim-stability 还是 `upgrade_rule` 的三条证据线之一；全部改走 `cc.metric_label()`，两条 docstring 补「口径由 --metric 定，默认 RMSE」。新增两条守卫：会画到 PNG/写进 note 的口径名必须来自 metric_label（放过 `--metric` 自己的 help 与固定列名），docstring 不许把默认口径说成唯一口径 | 用户："所以主要bug就是轮次没有隔离？其他没什么致命问题？比如跳步骤等？" → "按照你的顺序来" | 跳步骤这一类的真相是「散文说停、没人拦」：13 个停顿点里只有 5 个有机器闸，而今天补的 AA S3→S4 已在三个 blind run 里验证这套做法有效，剩下就是复制。R2-6 的根因是 schema 从未被写下来——不是校验漏了一条，是没有可校验的东西 | 验证：全包 770 绿 + 17 skip（含轮次隔离那批）；真实回归——r1 的账本现在在 Stage 1 就被拦（此前拖到 Stage 3 收口才发现），worst-slice-compare 在真长表上按 rmse/mse 各跑一次、note 跟着变且 perm_status 如实报 not-computed，生产者剧本 orient 输出改口、结论剧本输出不变
- 2026-09-09 | Weather 数据集上验收解释环收口：三个 blind run（sonnet 当主 agent，只给「跑 orient、照它打印的办」，全程不提 harvest_check 存在）。**Run A = 假设全落空**（同一份 Weather 切片版图、本轮只登记 H3 一条，干预跑完 undecided、17 个 real 切片一个没推动）：agent 自己回填 plan.script、独立复核 receipt、判 undecided、汇总 verdict_summary、跑 harvest_check 得 `harvest=none`，重跑 orient 被 `harvest-decision` 挡住，**停住没写 CONCLUSION.md、没替用户选**。**Run B = 部分解释**（完整 r1 账本 H1/H1b/H2/H3）：同样停在同一处，且**独立复现了 r1 的原始数字**（confirmed 1 / 推动 3 / 未解释 14），还自发按 §4 停顿条款给出了「各残留维度本轮没试过的取证角度」五条。**Run C = 用户答②以未决收口**：写出的 `## 模型结构依据` 首句就是「本轮未能归因到任何组件」、`## 已知缺口` 17 条切片名带前缀逐条列全，**conclusion_gate 一次通过没被弹回**（机器闸这一轮没派上用场，因为菜谱写清楚就够了；闸的拦截能力另由单测与 r1 结论回放证明）。**验收顺带抓到一条保真度 bug 并修**：`harvest_check` 把「账本登记为 uncovered」和「压根没进账本」印成同一句「无人认领」——Run C 自己发现 r1 账本的 `slice_map` 只有 29 行而 `slice_zcheck` 有 31 个切片（`month:2020-10`/`month:2020-11`/`time_half:first` 从没登记过）。现在 `claim_map` 出 `claim_state` 三态（claimed/uncovered/unregistered），报告分开印，未登记的 real 切片直接 exit 1 并点名（Stage 1 认领核对的硬规则本就要求三选一，漏登记是那道核对没走完，不是合法出口）；账本文件本身缺失也改成单独报错，不再伪装成一堆切片问题 | 用户："先跑 Weather 验收口" | 弱模型三次都走对了，说明收口不靠机器闸兜底也立得住；而机器闸拦下的恰恰是 r1 当初真的漏掉的东西。新增的未登记硬错**会让 r1 那份账本收不了口**——这是对的，那三条 real 切片当初就没走完认领核对 | 验证：全包 727 绿 + 17 skip；三个真实工作目录产物均在 scratchpad 可复查
- 2026-09-09 | 解释环收口（假设验证跑完之后那一步，此前完全没有）：新脚本 `scripts/harvest_check.py`——从 `slice_zcheck.json` 的 real 切片全集里减去被干预推动过的切片（`verdict_summary.slice_recompute` 的 `beyond_noise_floor`），算出未解释清单与残留维度分布，与 `verdict_summary.unexplained_real_slices` 逐条对账（缺字段/多列/漏列/三态计数与 interventions 不符/「零 confirmed 却零未解释」的自相矛盾，一律 exit 1 且不落盘），通过则追加写 `harvest.json`（数组，轮次自己数），并打印三档收成（`full`/`partial`/`none`）+ 逐条未解释切片 + 下一步三选一；维度只认切片名前缀不认账本 `slice_map.dimension`（r1 里 `month:2020-12` 被标成 `time`，照它分组会把同一批残留拆成两堆）。architecture-attribution：Stage 3 产物加 `harvest.json` 且加 `pause_after`，Stage 4 前置加新问题 `harvest-decision`（①换角度再取一轮 / ②以未决收口 / ③换方向停手，agent 不得自选），§2 加收口菜谱、§4 加 Stage 3 停顿汇报（每个残留维度各说一个本轮没试过的取证角度）、§6 加「空收成门」与 `## 已知缺口` 模板；conclusion_gate 规则 8（只在声明 `produces_ablation_receipts` 且盘上有 verdict_summary 时生效）——`harvest.json` 必须在且 `verdict_summary_sha256` 对得上（判定改过没重算即拦）、未解释切片逐条出现在 `## 已知缺口` 节（连 `channel:`/`month:` 前缀原样抄）、`n_confirmed==0` 时 `## 模型结构依据` 节必须含「本轮未能归因到任何组件」、证据清单必列 `harvest.json`；engine-core 假设验证循环补 ③收口/③′再一轮两步 | 用户："包括如果我第一轮假设没有发现任何有用的点，是否会重新再跑一轮" → "OK完全没有问题，直接搞" | 改进环有整套轮次机器件（new-round/stagnation/budget/max_rounds），解释环一条都没有：engine-core 只有「refuted 且预算未耗 → 回生成器提修正假设」半句，且不覆盖 r1 的真实处境（1 confirmed、2 undecided、**14 个 real 切片没被任何干预推动**，然后直奔 Stage 4）。补的是收口不是自动循环——自动绕圈恰好会诱发剧本「事后编故事门」防的那件事，所以停下来把「还剩什么没解释」摆给用户，三条路都是合法结果。验证：scripts 467 绿 / chartbook 199 绿 + 17 skip / 全包 727 绿 + 17 skip（新增 test_harvest_check.py 16 例 + 规则 8 十例，含零破坏守卫）；**真实回归——把 r1 那份当初过了旧闸的 CONCLUSION.md 放进沙箱重跑，新闸直接拦下并点名 14 个该写而没写的切片 id**（r1 其实在 `## 消融证据` 节的散文里写了 13 个裸名如 `raining_s`，既不在缺口节也无法机器对回 `channel:raining_s`）；orient 实测 Stage 3 显示 ⏸、Stage 4 被未答的 harvest-decision 挡住
- 2026-09-09 | r2 三条高危修复（R2-1 / R2-7 / R2-8，验证前置）：**R2-1 目录型材料指纹**——`engine_common` 新增 `dir_fingerprint`（文件数 ≤200 逐个算 file_fingerprint 走 `dir-full`，更多退化成「相对路径+字节数」清单走 `dir-list`，遍历排序保证可复现，断链软链不计入）与 `path_fingerprint`（按 isdir 分派），`setup_manifest._inputs_of` / `product_manifest` 两处入口 + `engine_common` 过期比对处一并改走 path_fingerprint；`file_fingerprint` 收到目录改抛带指引的 IsADirectoryError（orient 的入场指引就叫用户整块拷 materials/，此前 data-setup 阶段直接崩）；**R2-7 Stop 钩子误拦生成器剧本**——「该不该有结论」的真相改为 playbook frontmatter（新增 `ec.produces_conclusion`：有阶段的 done_when.artifacts 含 CONCLUSION.md 才算），由 orient 落进 `diagnose_state.produces_conclusion`，hook 只读 JSON 不引 yaml；`PRODUCERS` 降级为「state 无该字段的旧工作目录」兜底并补进 model-comparison/fact-scan，新增守卫测试钉住兜底名单与 frontmatter 事实一致；**R2-8 素版候选入口三处**——`_switch_to_diff` 加 `SWITCH_RE` 校验（`--name` / `--name=值` 才收，「无（需改代码）」这类散文返回 None 由调用方跳过并打印原因），switches 分支补 `--target` 过滤（`_switch_targets` 读 model/target_model，整份表一条都没声明 = 单模型档案全放行，只要有一条声明就严筛），新增 `_is_noop` 与冠军 `champion.json.config` 逐键比对剔掉空转候选；三类跳过一律打印「跳过的候选：− …」不静默 | 用户："修。修完以后在weather数据集上跑一遍这个流程" | 三条都是 r2 联调实跑撞出来的，且都在验证路径上：R2-1 在 data-setup 就炸、R2-7 每跑一次 model-comparison 拦三回合、R2-8 是 auto research 素版路的正主（散文进 knob 名 / 别的模型的开关混进来 / 排出与冠军一模一样的空转候选）。**顺带修正一个错测试样例**：test_gate_guard 的 `mk()` 默认 playbook 一直是 model-comparison，拿一个不产结论的生成器剧本去测「该有结论却没过闸要拦」，前提本身是错的，改默认为 result-eval 后 5 个用例的语义才对得上。验证：scripts 441 绿 / chartbook 199 绿 + 17 skip（新增 R2-8 五例、R2-7 五例、R2-1 五例）；三条各自在临时工作目录实跑复核——目录指纹改内容即变、生成器 done 不拦而结论剧本照拦、三个坏候选各带跳过原因被剔且真候选保留
- 2026-09-09 | 选图门翻转的真实工作目录验证（复用 r2 setup 长表跑 model-comparison Stage 1）+ 验证抓到的三条修复：**①`build_index.py` 认现场写的图**——图 JSON 顶层有 `verification`（字符串或 `{tier: ...}`）即归「现场脚本（chartbook 未覆盖）」组、带 PNG 链接与档位标注，`exploratory` 档当场标「只作线索，不进结论」；两组都从同目录 `chart_plan.json` 自动登记「服务疑问」（脚本名蛇形 ↔ 产物名连字符经 `_norm_rid` 归一化），无计划文件时照旧不登记；**②`horizon-degradation` 补口径开关**——新增 `chart_common.metric_fn/metric_label`（按 row/model 之外维度池化的图共用，`row_metric`/`pooled_metric` 内部一并复用消重），该图 `--metric rmse|mse` + JSON 落 `metric` 字段 + note 与 Y 轴标题按口径生成（上一轮①补 8 张图时漏了它，因为它走自己的 `_rmse_by_step` 不调 `row_metric`，守卫测试够不着）；**③现场写图有样板了**——新增 `chartbook/adhoc-template.py`（`cp` 到 `analysis_scripts/` 改四处即可用），接好 chart_common 的中文字体/长表加载/口径开关/双落盘，`--verify` 跑对账两关并把 `verification` 写进 JSON，样板正文写明「关 2 手算不许调 compute 用过的口径函数——同一份代码复核同一份代码等于没查」 | 用户："修改，这一波修改完了以后，是不是基本上就没什么问题" | 验证按真实流程走完两回合：第一回合 orient 对声明图与可加画池一字不印、第二回合解锁并回显计划三条、`--strict` 在 recipe 未填时正确拦下；特意设一条 chartbook 给不了的疑问（按窗口内真值波动强度分层）走完「现场写脚本 → 对账两关 → 回写计划 → 出 INDEX」全程。**该现场图产出一个 chartbook 29 张都给不出的结论：TSMixer 的优势完全来自高波动窗口（L3 gap −0.1019），在最平稳的 25% 窗口反而落后（L0 gap +0.0043），四层等权均值 −0.02985 与 gap_summary 的 mean_diff −0.029833 守恒**——r2 里 worst-slice-compare 按月切时 concentration_ratio/perm 均为 null（焦点模型无正差距）看不出任何集中性，换个切法就清楚了，这是翻转价值的直接证据。样板自身的口径 bug（compute 走 mse 而 verify 手算写死 rmse）也是被对账两关当场抓出来的。验证：scripts 426 绿 / chartbook 199 绿 + 17 skip（新增 test_adhoc_template.py 4 例 + 索引 6 例 + 口径 3 例 + horizon 3 例）；真实数据复核——主口径 mse 下重画 horizon-degradation，交叉点仍为第 8 步（本例口径未改变结论，但开关与文案已同源）
- 2026-09-09 | 选图门翻转（菜单驱动 → 疑问驱动，先只接 model-comparison 一本）：frontmatter 加顶层 `chart_gate: sweep|plan-first`（缺省 sweep，旧剧本行为一字不变）；`plan-first` 分两回合——第一回合只写 `chart_plan.json` 的 `entries: [{question, evidence, recipe, source}]`（用人话问用户「这一步想弄清什么」，不许把 recipe 名当选项列给用户；上一阶段已有假设时取证目标就是「验哪条假设」不必问），**orient 在计划落盘前对该阶段的声明图与可加画池一个字都不打印**，第二回合才发图池去匹配；新脚本 `scripts/chart_plan.py` 校验（entries 非空 / question·evidence 各 ≥8 字且不许雷同 / source ∈ chartbook·ad-hoc / source=chartbook 的 recipe 必须是真 recipe id / ad-hoc 必须声明 verification / `--strict` 查开画前每条都落到了具体的图）；engine-core 的 chartbook 豁免补明「只管已覆盖的图，图池给不了的现场写进 analysis_scripts/」并新增**三档验证路径**（reconcile-2 纯聚合过对账两关可进结论 / chartbook-fn 检验调已验证函数可进结论 / exploratory 自写检验未验证只作线索，数字不许进 FINDINGS 假设条目与 CONCLUSION）；`_validate_frontmatter` 硬拦「plan-first 剧本声明 charts: 的阶段 done_when.artifacts 缺 chart_plan.json」；model-comparison 切 plan-first 并解开正文两处「全部用 chartbook 预写脚本」「palette 之外走可加画池」的锁死表述；_playbook-spec 记 chart_gate 字段与取证计划闸，subagent-briefs 的「四步清单」改为照 orient 当场打印的清单办 | 用户："我怕我们写好会限制agent想象能力…因为agent会偷懒，所以我想让他自己想" → "你这里需要确保模型没有skip这一步骤，因为我们prompt如果太长的话，有可能模型会把这步骤给直接跳过" | 原流程自相矛盾：orient 打印「默认全勾，别自作主张少画」而 model-comparison 正文写「没有疑问支撑的图不画」，agent 每次都选了当场打印的那条 → 从菜单里挑最接近的，需求落空且无人报告。防跳过不靠指令靠两道机器闸：**不给它看菜单**（信息呈现顺序强制流程顺序）+ 计划文件进 done_when.artifacts（跳过则阶段闸判未完成）。fact-scan 保持 sweep——它的活就是没有疑问时广撒网，翻转会把它废掉。验证：scripts 426 绿（新增 test_chart_plan.py 22 例，含「计划未落盘时图名与可加画池均不得出现」与 sweep 模式回归守卫）/ chartbook 183 绿；真实工作目录实测两回合打印与 fact-scan 无变化
- 2026-09-08 | 联调残留三条（用户点名按 1/2/4 顺序修）：**① 口径开关补全**——余下 8 张按逐行指标聚合的图（rolling-stability / oracle-gap / model-error-correlation / bad-window-clustering / good-bad-contrast / local-waterfall / feature-regime-error / feature-trend-overlay）全部接上 `--metric rmse|mse` 并把口径写进各图 JSON 的 `metric` 字段，能切口径的图 3→11；新增守卫测试（凡引用逐行指标的图必须有 --metric、不许再直调 row_rmse、必须落 metric 字段 + 数量下限断言），口径对齐硬规则补进 deployment-drift / result-eval / fact-scan 三本剧本（model-comparison 上一轮已有）；**② 判据②有闸了**——conclusion_gate 规则 5 增补：脚本判 `undecided` 而账本升级为 `refuted` 的假设（只可能走判据②预测落空），其 receipt 的 `pred_miss.eligible` 必须为真，旧回执无 pred_miss 字段同样拦；AA 剧本 Stage 4 同步写明；**④ model-improve 正文与代码对齐**——decide 挑冠军由「均值最小者」改为「按 metric.direction 取最小/最大」（代码一直是 max/min 分支，test_higher_is_better_picks_max_keep 早已钉住，错的只有正文），append 的封存键判据改为准确的「整键 test / test_ 开头 / 含 sealed」（原文「任何含 test 的键名」过宽，latest_ckpt 这类正常字段并不会被拒）并点明 confirm-round 就是门 2 的机器实现；冒烟 README 的 root_path 值补正（比 lsf_mini_dir 深一级，指向 dataset/）| 用户："Ok 所以现在还有bug吗？" → "按 1、2、4 的顺序继续修" | ①是 F7 的完整版：上一轮只修了三张证据线图，其余八张仍会用另一个口径答题且不报错，而 rolling-stability 是 deployment-drift 的必画图；②补上「有信号没有闸」的缺口——pred_miss 记了却没人读，等于没有强制力；④是上一轮终审记下、一直没动的三条文档漂移。验证：scripts 404 绿 / chartbook 183 绿 + 17 skip；真实产物复核——真实 AA 工作目录仍过闸（无判据②升级，新闸不误伤），真实长表上 rolling-stability 两个口径都跑通且数值不同（首日 TSMixer 0.254 vs 0.091）
- 2026-09-08 | 全链路联调 F1–F12 一次修完（Weather TSMixer vs NLinear 真跑出来的链路问题）：**F1** hooks/gate_guard.py 只在「已落 CONCLUSION.md 而 receipt 不合法」「state 缺失或自称 done 却无 receipt」时打回，流水线中途（pause_after 停顿、等 subagent）放行，且逐个工作目录判定（跑完的旧目录不再替别人挡路）；**F2** _agent-spec §3.1 + 三张 producer 卡片：产物落盘后必须再跑 orient 把 current_stage 推到 done 并写进 verification；**F3** model-audit/output-spec：pointer 写 `<workdir>/references/`，明令禁止写进引擎包；**F4** cross-skill-contract：H-ID 登记表改写工作目录副本，引擎包那份降为只读格式样例；**F5** chartbook/build_index.py 加 `--out`（图链接按 out 目录相对化，跨树用绝对路径），四本图剧本 + orient/engine-core 的命令统一写 `--out INDEX.md`（阶段闸判的是工作目录根部）；**F6** chart_common.resolve_freq：`--freq` 缺省从长表同目录 setup_manifest.json 读真实步长，五张按物理时刻聚合的图接上；**F7** chart_common.row_metric/pooled_metric + cross-dim-stability/worst-slice-compare/model-rank-significance 加 `--metric rmse|mse`，model-comparison 的 gap_metrics CLI 加 `--caliber`（默认必须留 rmse_192 供金标准），upgrade_rule 加「三图 metric 与 gap_summary caliber 同源」；**F8** gen_gate 支持 manifest 的 `covers`：脚本不在本 stage golden 覆盖范围内时报「无 golden 覆盖」退出码 3、不写闸报告（AA stage 0 只钉 slice_zcheck）；**F9** models-template/output-spec 的 ablation_switches 增 `values` 列（config-flag/code-stub 必填），experiment_log candidates 按 values 逐值排候选；**F10** ts-train-batch.js 按 task 分字段白名单（intervention 透传 switch/kind/seeds/pred_direction/两条判据/噪声底/基线）+ args.context 共用上下文；**F11** ablation_verdict 加 `--delta2/--noise-floor2/--caliber2` 与 pred_miss（判据②缺第二口径恒 false → 只能 undecided），AA worker 卡片红线「删预测数组前先算第二口径」；**F12** provenance `--serves` 允许一支脚本挂多个假设（`eval_H1.py=H1,H1b`，逗号/重复都合并），conclusion_gate 规则 5 按 episode_ids 判 | 用户："OK可以继续全链路联调看看有没有额问题" → "OK你先修复这部分的问题" | 十二条全是「单跑分测过、串起来才暴露」的接缝：闸与剧本各自自洽但互相错配（F5/F8/F12）、口径/步长在层间丢失（F6/F7）、派卡字段在 workflow 处被裁掉（F10）、产物被过早清理导致判据永久不可复核（F11）。验证：scripts 396 绿 / chartbook 170 绿（-W error::UserWarning）；真实产物端到端复核——AA 工作目录去掉当初为过闸而写的 eval_H1b.py、改回 `--serves eval_H1.py=H1,H1b` 后结论闸通过；build_slice_metrics.py 过 gen_gate 得到退出码 3 而非误报 FAIL；**按本次真实主口径 mse_96 重画 cross-dim-stability，NLinear 对比从 time_stable=false/caliber_stable=false 翻成 true/true——F7 当时把「两条正交切分都不稳」这个结论级判断算错了**
- 2026-09-07 | 改进环 + Phase 4：新 playbook model-improve（按轮批跑候选、冠军、四条停止规则、封存终评）、scripts/{evaluator,improve_verdict,experiment_log}.py、账本 kind=improvement/untested、conclusion_gate 规则 7、engine {round} 占位 + json: DSL、model-improve-worker 卡、workflows/ts-train-batch.js、eval-cases/adapters/lsf_mini_adapter.py + Weather 冒烟 | 用户："我想做一个 auto research 的框架……发现能改进的地方，然后我们去改进，然后进步" | 解释环只到组件为止；改进环把「改了会不会好」做成代码守规则的循环，与消融验证共用 worker 形态，第一条链只接 model-comparison
- 2026-09-07 | 终审修复：卡片契约加 verification 回传、图表选择门归主 agent 派前过、单写者规则与卡片一致（卡片跑 orient/追加现象行为例外）、六个 playbook 派发措辞按名字派卡、orient 菜谱抽取失败降级为警告并打印菜谱通则、ENGINE_DIR 用 realpath（symlink 安装下 project-context 可达）、install.sh --check 校验链接指向本包 | 全分支终审 8 条 Important | 卡片层与派发散文在四处不一致，弱模型保障要求硬规则必须打印到眼前
- 2026-09-06 | Phase 2 缩短：orient 前置齐时打印当前阶段菜谱原文（recipe_sections + 每阶段恰好一节守卫）、engine-core 5269→2714 token（只留代码查不了的判断规则，预算 ≤4200 守卫）、SKILL.md description 603→220 字符只留触发短语（skill 列表预算 = 上下文 1%，最少使用者被丢） | 用户："my playbook is too long, will this affect the skill performance?" | 弱模型失效 = 拿着整份 playbook 与 6.7K 的散文只执行一支；07-24 三轮加固证明「打印到眼前」才绑定；内容一字不删，只改出现时机
- 2026-09-06 | Phase 1 打包：agents/（v2 的 11 张卡移植 + architecture-attribution compute/worker 两张新卡 + worker 模式守卫）、hooks/ 迁入包内（根目录 symlink 保外部路径）+ hooks.json + install_hooks.py、install.sh/INSTALL.md、派发层改卡片制（engine-core/subagent-briefs/batch Phase C；未注册回退 general-purpose）、ts-diagnose-v2 删除 | 用户："divide ts-diagnose into different folders … so ts-diagnose will be a package include everything like agents, workflow I need for this task" + 拍板 Claude Code 宿主 / symlink 安装 / 删 v2 | v2 已落后 v1（12 playbook 文件、5 脚本有差异）；只有 SKILL.md 是 Claude Code 意义上的 skill，卡片与钩子在原生目录才有工具锁死/无提问权/Stop 拦截；回退规则让同一套卡片在非 CC 宿主可用；实测：~/.claude/agents 下的 symlink 卡片会被 Agent 工具识别为可用类型、hooks 设置改动即时生效（无需重启会话）、Stop 钩子与回合提醒均已在工程会话里触发
- 2026-08-31 | conclusion_gate 规则 5+6（溯源闭环+证据清单，同规则 4 的 produces_ablation_receipts 作用域，6 个非 pilot playbook 零破坏）：每张 receipts/H*.json 必须是 ablation_verdict --out 的完整 schema（produced_by+script_sha256 在盘且指纹相符、seeds≥3），provenance.code.serves 必须挂回对应假设，intervention_plan 已执行条目 script 必须回填；CONCLUSION 必带 `## 证据清单` 节逐条点名证据文件、盘上每张 receipt（含被否证的）必列防摘樱桃。playbook Stage 4/§6 同步 | 自进化第 0 步：C1 拼图 16 条 gap 全部源于"引用行为靠自觉"（H2 手搓 10 字段薄回执、8 个孤儿脚本、结论无证据清单） | 把 Stage 3/4 已有散文纪律变成机器拦截；轨迹图（skill-evolve graph/build.py）按这些字段确定性收割依赖边，gap 归零后反思不再靠读文件猜依赖
- 2026-08-03 | 新增 Layer -1 批量编排层（scripts/batch.py + references/batch-orchestration.md）：同一份数据一次并行诊断多条 level-2 playbook，生产者跑一次、提问一次、计算 fan-out 到事实提取、合并停顿、结论仍主 agent 落笔 | 用户问"如何同时跑多个 playbook、subagent 无提问权下消息如何回传" | subagent 无提问权是硬约束，唯一解是把交互全提到主 agent 的两个合并停顿点，重活各吃各的 256k
- 2026-07-14 | 首版落地（机制层 + SKILL + references 五篇 + playbook 三个） | 用户："现有技能针对特有情况写死，需要提取 workflow 做泛化技能，不确定时必须问用户，且要能固化回专用技能" | 引擎+playbook+profile 三层：workflow 与领域解耦；设计 spec 见 docs/superpowers/specs/2026-07-14-ts-diagnose-engine-design.md
- 2026-07-14 | 验证记录 | —— | 机制层 pytest 23 项全绿；training-sufficiency 合成数据影子验证全程走通（植入效应 m03/m08 均召回、五个验证步 PASS、必答题零跳过）；robustness / feature-importance orient dry-run 通过；crystallize 固化演练三步验证（冷启动 --profile / 快照冒烟 / 影子重跑 Stage 0 一致）全 PASS
- 2026-07-14 | 加固轮（三层加载/两道闸/三关判据/路由消歧/接口版本化，spec：docs/superpowers/specs/2026-07-14-ts-diagnose-hardening-design.md） | 用户："这些建议非常好，可不可以借鉴一下加强 skill？不一定全用，挑好的 idea" | SKILL.md 压成 41 行纯路由层（执行细节下沉 references/engine-core.md，test_layering.py 守预算 ≤60 行/~6K token）；playbook 目录化并各自带 golden/ 金标准（确定性植入效应）+ gen_gate.py 生成闸（金标准算错不许碰真实数据）+ provenance.py 归因闸（结论必附代码/数据 hash）；crystallize 升级三关判据（crystallize_gate.py 可执行：多样性 N≥3/ts=5、held-out、快照自洽）；路由优先级进四技能 description（test_routing.py 漂移守卫）；profile 的 experiment_line 打 interface_version: v0-draft 占位不生效。裁剪：模拟打分路由器（伪测试）→ 存在性守卫；held-out 场景库 / regression baselines 只预留接口位置（spec §6）。pytest 56 项全绿
- 2026-07-22 | 引擎 v2 框架：materials 盘点（11 类+三值状态+material: DSL+orient 盘点段）、contexts provider_skill 嵌入执行、profile 固化 materials | 用户："所有 skill 开始时都该搞清楚我们有什么…如何让一个 skill 调用一个 skill 然后回主流程" | 泛化 intake 与 skill 委托，为 chartbook（Plan 2）与 model-comparison/fact-scan（Plan 3）铺路
- 2026-07-22 | v2 chartbook 轮（A-C 组）：新增引擎级图谱库 chartbook/（规范长表 window_ts|unit_id|model|horizon_step|y_true|y_pred、chart_common 公共件、_recipe-spec 规范+一致性闸、9 个预写图脚本 A:error-breakdown/intraday-profile/worst-points B:horizon-degradation/rolling-stability C:true-vs-pred-scatter/model-error-correlation/worst-slice-compare/oracle-gap 全部合成植入回收 golden、example_adapter 对账两关样例）；engine-core 增 chartbook 豁免（预写图禁现场重写）；test_layering 守卫扩展至 chartbook | 用户："这些图工作量特别大，是要把代码写好吗，还是跑这个 skill 时现场写？"（决策：预写） | 预写+pytest 一次验证胜过每次现场写；JSON 一等产物/PNG 副产品；现场只写薄适配器过对账
- 2026-07-22 | v2 Plan 3 收口：D 组 3 图（feature-error-conditional/feature-trend-overlay/y-vs-feature-mapping）+E 组 train-test-drift、playbook 阶段级 charts 声明（orient 逐图可画性）、model-comparison 五阶段 playbook+fact-scan 体检 playbook（各带 golden 过 gen_gate）、路由表加两行+pv-result-analysis 反向让路 | 用户："用户进站直接跑 /ts-diagnose…比如用户想问模型A为什么比模型B要好" | 完成"盘点材料→自动画图→对比归因"闭环；体检与诊断分离（fact-scan 无结论阶段）
- 2026-07-23 | 证据可靠性加固：worst-slice-compare 内置按日块置换基线（统计量=最差片正差距，未过基线不点名切片）+ 新图 cross-dim-stability（时间对半/口径切换正交稳定性）+ mechanisms §2「证据维度」纪律 + model-comparison 升级三条腿（两线一致·噪声门·时间稳定；口径翻转限定口径不阻塞）+ golden 固定种子豁免边界 | 用户："我觉得你说的两个 suggestion 非常好，帮我写一个 spec" | 堵多重比较虚警与多图同源伪收敛；spec：docs/superpowers/specs/2026-07-23-ts-diagnose-evidence-hardening-design.md
- 2026-07-23 | 弱模型压力测试修复轮：test_layering/test_gen_gate 改动态发现（PLAYBOOK_IDS 目录扫描+下限断言、REFS 从 manifest reference 字段收集，新 playbook 不再静默脱守卫）+ spec §4.8 chartbook 覆盖声明硬规则（逐 recipe 声明或跳过）+ spec §5 植入难例形态规则 + spec §7 default 与菜谱同法自查 + mechanisms 反驳门 8「渐变-突变混淆」（时间定位双点齐报）+ engine-core 对账两关扩到一切整形脚本 + 停顿点模糊授权操作判据 + 新 playbook deployment-drift（部署后退化/漂移：切分点+onset 首离双点、置换基线、诱因双关筛查、ramp 难例 golden） | 用户："do a pressure test with another question…switch to sonnet…find out what need to be changed" | Sonnet 全流程压测（非光伏负荷退化+埋点数据）：路由/授权/新写 playbook/闸全过，但 onset 答错一周仍标高置信（切分点≠起始点）、日内维度整段漏查（无 chartbook 清单强制）、硬编码守卫盲区（GAPS G-1~G-7）；修复全部落 CI，pytest 263 绿
- 2026-07-23 | 图表选择门（画前增删图，engine 级）：任何声明 charts: 的阶段画图前必停一次 AskUserQuestion 多选——默认全选可画图（被动接受=画全套，守完整性），用户可删图（记 PROGRESS+CONCLUSION 声明覆盖缺口）或从「可加画池」加图（跨 playbook 任取材料满足的 recipe）；选择门=画前定范围 vs pause_after=画后定深挖，二者不合并。engine_common 加 declared_recipes/addable_recipes/has_chart_stage/recipe_min_models，orient 打印三组图+可加画池；recipe frontmatter 加 needs_models（对比类图=2，可加画池标注「需 ≥2 模型」防单模型误加，<N 运行时 ValueError 兜底），4 对比图（worst-slice-compare/oracle-gap/model-error-correlation/cross-dim-stability）已标；spec §4.8 补选择门与可加画池的衔接 | 用户："we might want to delete or add extra images for final analysis task"（压测追问后确认）+ 选 draw-all-default 与 add-any-applicable | pytest 266 绿

## 2026-07-23 chartbook 扩展第四轮(呈现层收口,28 图工程完结)
- orient 图表选择门按六类 category 分组:声明图带类别标签,可加画池分组呈现
  (engine_common.recipe_category);
- 新增 `chartbook/scripts/build_index.py`:扫产物目录生成 INDEX.md,按类别分节、
  只索引实际产物(28 图是库存非必画清单,不为没画的留空位);
- true-vs-pred-scatter 增强:Mincer-Zarnowitz 回归(y_true=a+b·y_pred)+
  a=0,b=1 联合 F 检验注记,配对正交噪声零随机精确 golden;
- Plan3 延后清单清扫:死 pandas import×4、背景集退化分支如实标 all-windows、
  双 RNG 播种注释、tests/conftest.py 集中 OpenMP 豁免(全套件回到 0 warnings)、
  lookback ref≈0 与负 φ 瀑布渲染补覆盖。

## 2026-07-23 chartbook 扩展第一轮:地基

- recipe frontmatter 强制 `category`(六类,engine_common.CATEGORY_IDS,conform CI 闸)
- id 领域名词闸(weather/station/solar/irradiance)+ 领域中立硬规则进 _recipe-spec §5.6
- `setup_font` 硬化为 CJK 回退链(返回命中列表);回填 row_analysis / analyze_row 两外围脚本
- 28 图扩展总设计:docs/superpowers/specs/2026-07-23-chartbook-expansion-design.md

## 2026-07-23 chartbook 扩展第三轮:模型归因层

- predict_adapter 契约进 _recipe-spec §6(CAPABILITIES+predict(requests)+get_model 白盒路);合成线性/lookback 双 golden 适配器
- attribution_common:加载校验、BudgetedAdapter(预算+请求哈希 jsonl 缓存)、background_set(kmeans-medoid,meta 落盘=守卫一)、feature_corr_groups(守卫二)
- 三图:global-attribution(KernelSHAP 双面板,梯度白盒路 --explainer auto)、lookback-decay(按桶遮蔽+逐窗有效历史)、local-waterfall(单行 φ 解析回收+防冤枉诱饵)
- chartbook 25→28 recipe,attribution 类首次有图,六类全满;依赖新增 shap==0.44.1(numpy 必须保持 1.26.4)
- 2026-07-24 | 弱模型加固 A+B：orient.py 目标阶段块打印「引擎级恒问五类·开工前自检」横幅（schema/判据/降级/破坏性/多版本，playbook 没声明也提醒——恒问五类此前只靠模型自觉，orient 不主动surфacing）+ 输出末尾打印「每回合先跑 orient 再动手」脚注；engine-core Step 0 增「引擎第一纪律：每回合先 orient 再动手、做完重跑、不凭记忆推进」（test_engine 新增横幅+脚注断言，109 绿） | 用户："我要用 256k 的 deepseek flash 弱模型跑这个 skill，检查是否够频繁用 subagent、小模型能否不漏步跑通" | 弱模型两大失效模式=不 notice 该问就假设开工 + 跑一次 orient 后脱离清单凭记忆推进；A 把恒问五类摆到每回合眼前，B 把 orient 重跑立为第一纪律并每回合脚注提醒
- 2026-07-24 | 弱模型加固 C：密集散文改即时打印编号清单——orient.py 图表选择门印 4 步编号清单（多选默认全勾/删图不静默记 PROGRESS+CONCLUSION/可加画/画完出 INDEX 再停顿），产 CONCLUSION.md 的结论阶段印「三道门自检」清单（门1 稳健·门2 假设登记·门3 反驳门 + 证据线/功效/provenance 收尾，just-in-time 不靠模型追 mechanisms.md 指针）；engine-core 同步两处 prose→编号清单（test_engine 三道门正/负例、test_charts_decl 清单断言，110 绿） | 用户："do c and commit and push"（弱模型加固 A+B 之后） | 弱模型失效模式之一=多分支密集散文只执行一支、指针懒得追；把最判断重的两处（画图门/结论三门）在该动手的那一回合直接打到眼前
- 2026-07-24 | 单入口化收口：四个 pv-* 专用技能（pv-result-analysis/pv-station-influence/pv-model-analysis/pv-feature-blame）方法全部收编为引擎 playbook（result-eval/subset-influence/model-audit/feature-importance 扩展含特征质量归因与反事实），技能壳删除；SKILL.md description 删除全部「负面清单」与 pv-* 让位条款，改为正面枚举 9 个 playbook 触发词；路由优先级由三级（固化代理 > 专用技能 > 引擎兜底）收成两级（固化代理 > 本引擎）；路由表补齐 result-eval/model-audit/subset-influence 三行并清理各行残留的「非光伏标准评估场景」措辞（收编后不再有对照对象）；test_routing.py 随删壳精简后再加 5 项断言（description 枚举全 9 playbook、无负面清单残留、优先级两级措辞、SKILL.md ≤60 行覆盖全 9 playbook）；CHANGELOG/README 同步 | 用户："do task 12"（四技能收编 Task 5/7/8/11 之后的收口任务） | 收编完成后 description/路由表若仍挂着已删技能的让位条款，是死链接且误导路由判断；两级优先级是收编后的真实状态，三级是历史包袱
- 2026-07-24 | 验证记录：弱模型场景端到端冒烟（三道闸复演本项目起因的翻车路径，临时目录外仓跑，见 .superpowers/sdd/task-13-report.md） | —— | 四步全 PASS：①空目录 `orient.py --playbook model-comparison` → 输出含「BLOCKED: 材料盘点未完成」且不含任何 `Stage ` 菜单行；②五件套（training_log/train_y/checkpoint/model_code 皆 absent-confirmed+source=user+degraded_ok，truth 连同 required 的 predict 均 present 带 paths+schema.y_col+schema.time_col）落盘后重跑 orient → 「Stage 0 口径与对齐 [← 当前]」连同完整阶段菜单打出；③同状态下 `orient.py --goto 4` → 输出含「⛔ 拒绝直达 Stage 4：前置不齐」；④CONCLUSION.md 故意不写「## 模型结构依据」节，跑 `conclusion_gate.py` → 打印「缺「## 模型结构依据」节」且进程 exit code=1。全仓终验：`python3 -m pytest ts-diagnose/ -q` → 353 passed（0 warnings）

## 2026-07-25 分层机制 Phase 1（produces/upstream）

- 新机制：playbook 可声明 produces（产物）与 upstream（依赖）；orient 机器裁决——
  required 缺 → 自动内联生产指令并阻塞开工；optional 缺 → 三分支问；输入指纹过期
  检测（stale 须用户确认）。加载期校验：产物 id 唯一、引用存在、依赖无环、问题去重。
- 新 playbook data-setup（必需前置）：适配器+对齐+setup manifest（含材料清单与
  训练日志位置，全下游复用）；golden 植入不对称覆盖难例。
- model-audit 升格生产者（model_profile）；fact-scan 消费 setup、产 chart_sweep。
- 试点 model-comparison：图 14→5（对比核心集），阶段 5→4，contexts 改 upstream。
- 向后兼容：无 upstream 声明的 playbook 行为不变；contexts: 迁移期保留，Phase 2 退役。

## 2026-07-25 分层机制 Phase 2（批量转两层 + contexts 退役 + metric-eval，全量落地）

- 剩余 6 个分析 playbook 批量转两层（逐个 task，模式照 model-comparison 试点）：
  result-eval（新产物 eval_report，图 11→5）、deployment-drift（适配器出 Stage 0
  归 setup，图 10→4）、robustness（基线复算改消费 setup 长表）、feature-importance
  （对齐让渡 setup，features 升 required）、subset-influence（heldout-eval 上下文
  改 eval_report 产物三分支）、training-sufficiency（setup 作可选加速器，日志位置
  从 manifest 免问）——至此 7 个分析 playbook 全部声明 upstream，`contexts` 全零命中。
- contexts: 机制退役：engine_common 删 context_status/context_embed_hint 两函数、
  orient 删 contexts 循环、test_engine 删配套用例；upstream 产物机制全面接管。
- Phase 1 终审移交加固五件：尾采样钉、optional-stale 钉、modelmap declined 钉、
  window_range 时间序修正、chart_sweep→setup 依赖边补齐。
- 新生产者 metric-eval（产物 metric_table）：只算指标不归因，口径由用户指定
  （默认 rmse_192）；result-eval/model-comparison 声明其为 optional 上游，口径
  匹配即复用免重算。引擎 4 生产者（data-setup/model-audit/fact-scan/metric-eval）
  + 7 分析 playbook，共 11 个 playbook。
- 文档收尾与裁决落笔：question-discipline.md 补「上游产物拥有的问题」一节
  （生产者拥有领域问题，消费者不得重复声明同 id 问题，validate_upstream 机器拒绝）；
  crystallize.md 补一行（products 登记不入 profile，同 degraded_ok 现场事实不固化）；
  modelmap_blocker docstring 落笔裁决（不降级为 upstream 声明——variants 无法表达
  material 触发的 optional→required 升级）；README 两层表随全量落地更新（10→11
  playbook，metric-eval 生产者行，result-eval 兼产 eval_report 注记）；
  phase2-TODO.md 转为完成记录（保留文件作沿革）。
- 全阶段零 golden stage 键平移（各 playbook 只挖改/收窄内容，未触发阶段重编号）。
- 验证：`python3 -m pytest ts-diagnose/scripts/tests -q` 全绿；`grep -rn "contexts"
  ts-diagnose/playbooks/*/playbook.md` 零命中。

## 2026-07-28 生产者外包机制（Brief-PRODUCER + 编号菜谱 todo 纪律）

- 触发反馈：用户提议"能否预设 subagent 角色，每次 setup 都由 subagent 执行"；讨论收敛为
  分层原则——生产者机械执行外包、结论永远主 agent 落笔，且模板内嵌 playbook（单一事实源、
  零安装可移植），注册 agent（SDK `agents` 参数/`~/.claude/agents`）仅作可选增强。
- subagent-briefs.md 新增 **Brief-PRODUCER**（生产者整体外包模板）：三条适用条件
  （produces + 无结论阶段 + 执行段无用户裁决/FINDINGS 写入）；data-setup、metric-eval
  满足→整体外包；model-audit（多候选裁决+自检 subagent_ok:false）与 fact-scan
  （图表选择门+现象清单）不满足→维持各自 §5 细粒度拆分。
- engine-core 三处：subagent 编排补分层原则；执行模型补"编号菜谱逐条建 todo、
  【硬规则】步不得合并跳过"；常见错误"内联生产不得丢 subagent"修订为"没收齐 questions
  答案不得丢"（提问在主 agent，答案齐后机械段可外包）。
- data-setup / metric-eval：Stage 0 菜谱重排为编号步骤（步 1-2 主 agent 提问+硬规则派发，
  含自查绊线"发现自己在写 adapter.py/metrics.py 即派发被跳过"；步 3 起 subagent，
  Stage 1 subagent 承接）；§5 从"不拆"翻转为整体外包硬规则；§4 补"消费者为用户本人时
  主 agent 读落盘产物补充细节"；对账/闸数字随回传由主 agent 记 PROGRESS（单写者不破）。
- orient.py：playbook 声明 produces 时打印 🤝 生产者提醒（提问在主 agent、机械段按 §5
  派发、回填 config.products）——机械输出钉在决策时刻，不指望模型记得 prose。
- 验证：`python3 -m pytest ts-diagnose/scripts/tests -q` 164 全绿。

## 2026-07-30 全库文档"说人话"改写（结构不动、规则不丢）

- 触发反馈原文：用户："description 太冗余……我看不懂它怎么工作的。用容易理解的句子，
  但要 clear 和 critical。说白了就是说人话。不要改结构。"
- 范围：SKILL.md description 压缩（触发词与 11 个 id 全保留）；_playbook-spec.md 全文
  重写（frontmatter 字段逐组拆讲、指纹/产物/难例规则拆成短句）；engine-core.md 全文
  重写；mechanisms / intake / crystallize / _recipe-spec 最密段落拆写；11 个 playbook
  正文及其 references、chartbook recipes 正文按同一风格契约改写（frontmatter 机器读
  部分零改动）。
- 纪律：只重写句子，不动结构与规则；命令/路径/阈值/保留字原样。
- 附带：仓库 .gitignore 增补运行时产物（gate_reports/、.pytest_cache/、
  diagnose_state/config、provenance、crystallize_record）——运行产物不属于技能本体。
- 二轮收紧（同日，触发反馈："I don't want any explanation in this skill folder…
  waste token"）：删除首轮加入的解释性内容——名词速查/术语定义、"教训"故事（只留
  提炼出的规则）、设计动机旁白、超一行的文件用途介绍；句子仍是自然语言，不回退缩略。
  全库字数回落到 HEAD ±2%；frontmatter 与跨 playbook id 引用逐文件核对无变化。
- 验证：`python3 -m pytest ts-diagnose -q` 全绿（见本次会话）。
- 2026-08-17 | tier2-c1 实跑回顾（未改菜谱/脚本，仅记录）：①orient --playbook 对已绑定 config 不改绑——转验证主脊需手改 config.playbook，建议 orient 加 --rebind 显式动作；②QUESTION 侧噪声底为 ddof=0 口径、菜谱写 ddof=1，本次以双版噪声底皆过为判定条件化解，建议 noise-floor 问题追问 ddof 口径；③干预已预跑（零新训练）场景下 Brief B 的"重训"字样需按"评估既有产物"适配，本次已在派发时改写；④收敛型（null-pred）假设与 ablation_verdict 三态的语义映射靠 plan 的 confirm/kill_criterion 预注册兜住，建议 verdict 增加 converge 方向枚举。触发反馈：盲跑 runner tier2-c1（QUESTION.md 驱动、无交互提问通道）。
- 2026-08-20 | round-1 反思循环编辑 CE-7~CE-17(11 条,均为追加/收紧型,不削弱既有纪律):architecture-attribution——Stage 0 分桶粒度判则(周期维逐小时、同号才许合并、等宽桶对半细分核查);Stage 1 切片认领核对第三步(real 切片三态:认领/同源标注/uncovered)与起草要求④(claim 断言须有判据覆盖);Stage 3 切片重算硬规则(干预产物落盘后在全部 real 切片重算 delta,不新增训练)、refuted 判据②预测落空(|delta|≤噪声底一半+双口径同判)、措辞-状态同源、收敛终止条件核对;Stage 4 数字复算步;§6 追加背景陈述门/状态口径门/数字复算门三门。model-comparison——Stage 2 切片认领规则(slice_map 全维收录+反向切片须认领)与 claim 字段判定范围约束。触发反馈:skill-evolve round-1 三案盲跑评分(C1 0.97/C2 0.795/C3 0.985)的三份无记忆反思归因,提案全文 skill-evolve/loop/round-0001-edit-proposals.md。验证:python3 -m pytest ts-diagnose -q 450 绿。
- 2026-08-21 | round-2 反思循环编辑 CE-18~CE-24(7 条,追加/收紧型,唯 uncovered 字段名为替换):architecture-attribution——Stage 1 起草分支追加互补假设预登记(`H<n>b`:同组件同干预、判据取反、pre-registered、共用 receipt)与账本字段名归一(读入 not_registered 先改 uncovered);Stage 3 切片重算追加三态追问(归零/反转/残留进 verdict_summary,证据挂认领假设名下,无认领者补 provenance:"complement" 受限互补假设)、升级披露完备性(脚本判定与账本不一致逐条披露,kill_receipt 引用切片须与预登记点名一致)、post-hoc 墙加 complement 例外引用;§6 追加头条对账门(执行摘要 confirmed 清单与账本一一对应、否定式断言只覆盖 refuted、细化假设并置母假设边界)与版图降级门(选择性剔除=集中度检验不得推翻 real 切片方向,推翻只认同口径跨种子重算)、数字复算门追加阈值概括判则(「X 以上」须全部实测值满足)。model-comparison——Stage 2 切片认领规则:not_registered 替换为顶层 uncovered(单一字段名)、追加互补假设预登记同文。触发反馈:skill-evolve 反思②双反思器归因(C1 跨版本 V1 0.65/V2 0.94=同判则行为方差、C2 0.985 保留性),提案全文 skill-evolve/loop/round-0002-edit-proposals.md;CE-23 原提案文本因层级守卫(playbook 互不点名)改写为不点名版本。验证:python3 -m pytest ts-diagnose -q 454 绿。
