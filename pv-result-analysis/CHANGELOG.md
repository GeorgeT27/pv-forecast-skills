# 变更日志

<!-- 每次改动技能文件追加一行。格式：
     日期 | 改了哪个文件的哪节 | 触发这次改动的反馈原文（一句话） | 为什么这么改
     由 SKILL.md「运行后回顾」小节要求维护。变更可追溯 = 改错了能回滚。 -->

| 日期 | 改动 | 触发反馈 | 原因 |
|------|------|---------|------|
| 2026-07-08 | 新建 `references/hypotheses.md`；`SKILL.md` 加"假设登记 + 反驳门 + 运行后回顾"三节、两个 Playbook 收尾各接反驳门、references 表加一行、常见错误加两条；新建本 `CHANGELOG.md` | 用户问 AutoResearch/CORAL/GEPA 等论文技术能否用于本技能，提升"为什么 A 比 B 好"结论的可解释性与可信度 | 落地核心三件套：①假设登记表（DiscoveryBench 式预注册可判别预测，防事后编故事）②反驳门（多 agent 辩论验证的单机提炼，结论升级前强制排除替代解释）③GEPA 式运行后回顾（用文字反馈定向修正技能文件 + 变更可追溯）。均为流程纪律层，不动计算层 scripts/ 与指标口径 |
| 2026-07-09 | `SKILL.md` 新增"执行流程：四个阶段"总纲，Step 1/2/3、Playbook 加阶段标记，FINDINGS 状态枚举加"现象"；`pv-analysis-resume/SKILL.md` 跳过规则表加阶段入口说明 | 用户要求把一口气跑完的流程拆成四阶段（载数据算指标→相关性→事实提取→深归因），避免产出过长、避免在归因上浪费在用户不关心的现象 | ①Stage 3（只写现象 + 数字）与 Stage 4（才允许"为什么"，引 references + 假设 ID + 过反驳门）物理隔开，防事后编故事、把最贵的步骤只花在用户点名项；②Stage 3→4 设唯一强制停顿点，1→2→3 连续跑；③阶段进度靠产物判定，与续跑跳过规则天然对齐，不新增状态文件 |
| 2026-07-09 | `SKILL.md` 四阶段总纲加"ensemble 的分析边界"段 | 用户：ensemble 是均值组合、无独立机制，指标要报告但深入分析应聚焦 M1-M4 | ensemble 无独立特征（models.md 该节空），机制层只做 M1-M4；"ensemble 为什么好/不好"改由图#2 成员分散度 + 图#9 oracle 差距回答，不编独立机制故事 |
| 2026-07-09 | `scripts/plots.py` `fig02_error_corr` 长模型名改短编号刻度 + 底部图例映射，by_month 只首分面画 y 标签，stats.json 加 `labels` 映射（corr 键保持全名） | 用户：热力图行列标签是模型全名（fourierMoBA/PatchTST…），逐月分面时互相压叠成一团 | >4 字符名用编号，映射放图例；短名（M1/M2）保留原样；下游读 stats 的代码不受影响 |
| 2026-07-09 | `pv-analysis-resume/SKILL.md` description 加"直达 Stage 4 深归因"触发词，Step 2 拆成路径 A（可视化续跑）/ 路径 B（现象已看完直达 Stage 4） | 用户问：现象都看完了想只做深度归因（结合电站+模型讲为什么），怎么让 resume 触发 Stage 4 | 原触发词只有"画图/相关性"，深归因请求命不中；补入口的同时明确前提（FINDINGS 有带图链接的"现象"条目）、读什么（现象 stats + references 全套 + 假设 ID）、何时仍需重算（缺的图才补画） |
| 2026-07-09 | `pv-analysis-resume/SKILL.md` 路径 B 现象来源改为四级优先级（FINDINGS→ANALYSIS.md+stats.json 兜底重建并回填→纯 PNG+stats 现读→都没有退路径 A），Step 0 读结论同时读 ANALYSIS.md，归因所依据的核心图必 Read PNG | 用户：FINDINGS.md 是模型手写软产物、在另一台服务器跑没生成；那次有 ANALYSIS.md + 图 | FINDINGS 无脚本生成、不保证产出，硬依赖它会让路径 B 前提常失败退回重跑；ANALYSIS.md + stats.json 已承载现象，据此兜底重建并回填 FINDINGS 让后续会话稳定；顺带修正"新会话没画过图、只读 JSON 漏形状证据"——核心图必看 PNG |
| 2026-07-09 | `references/models.md` M1-M4 各加"代码类名"字段（FourierMobaTransformer / PatchRegForcast / MoiraiPvForecaster / PatchTSTPvForecaster）；`pv-model-verify/SKILL.md` 定位步骤改为类名 grep 首选、架构签名兜底，签名表补 M2 行 | 用户提供 M1-M4 实际代码类名 | 类名是比架构关键词可靠得多的定位符，pv-model-verify 可直接 grep 直达定义；M2 类名拼写 Forcast/Reg 与 M3/M4 不一致，记容错前缀 PatchReg 并注明以代码为准 |
| 2026-07-09 | M2 类名笔误修正 `PatchRegForcast`→`PatchRegForecast`（models.md + pv-model-verify） | 用户确认代码里是 PatchRegForecast，Forcast 是笔误 | 精确串修对，容错前缀 PatchReg 保留 |
| 2026-07-09 | `SKILL.md` 新增"面向主管的结论汇报（CONCLUSION.md）"一节 + 四阶段表 Stage 4 交付物加 CONCLUSION.md；`pv-analysis-resume` 加"只写主管总结"路径与触发词 | 用户：ANALYSIS.md/FINDINGS.md 很好但太严谨、表格太多，主管读不直接；需要一份面向主管、有数据支撑但逻辑清晰的 conclusion.md | 三文档金字塔按读者分工：CONCLUSION 面向主管=把已成立结论翻译成管理层语言（叙述优先、全文表格≤1 张、方法论机器不进正文、金字塔先结论、结尾给建议、只写站得住的），不产生新结论只综合 FINDINGS 已证实条目 |
| 2026-07-09 | 天气分型从四类改五类：`data_utils.daily_weather_class` 启用此前未使用的 kt_lo，阴稳改为 kt<0.35 的真正厚云阴天，新增"多云平稳"（0.35≤kt<0.65）默认档；`plots.py` fig08 order、SKILL.md 三处、scripts/README、models.md/hypotheses.md 序号引用同步 | 用户发现 kt_lo 参数定义了却没用、"阴稳"实为兜底类（中等 kt 的日子也被划进阴稳）；要求改成真正的阴稳 | 低波动日按 kt 分三档（晴稳/多云平稳/阴稳）语义更准，图#8 归因与占比分解更清晰；"第四类"等位置序号改为"突变日组"避免与类数耦合；合成数据验证三档 kt 边界正确、fig08 五类出图正常 |
| 2026-07-09 | **设定变更：5 站 2025 联合训练 → 留出测试站雅砻江（零样本跨站迁移）**。全线改口径：`SKILL.md` 项目背景（跨站主线、测试单站雅砻江、config 加 train_stations）；`models.md` 全局约定（2024训练→5站2025联合/留出雅砻江）+ H-M1-2 DoY 跨纬度海拔失配 + RevIN 跨站护城河；`hypotheses.md` H-M1-2 重述 + 新增 F 组跨站迁移（H-XSTN-1 高原带 OOD、H-XSTN-2 RevIN 尺度迁移、H-XSTN-3 重气象特征更站无关）；`station.md`/`seasonality.md` 重写为 6 站（泗洪/小壕兔已联网查实、白马湖已有、润达/和熙标待补、雅砻江=留出）+ 气候带 + 谁最像雅砻江；`drift-and-nwp.md` 跨年→跨站（5训练站 pooled+逐站 vs 雅砻江）；`run_drift.py` 加逐站 PSI 对比（找迁移锚）；`data_utils.weather_class_drift`/`plots.fig11/12` 的 "2024/2025" 标签与 stats 键改语义中性的 `train/test`（旧标签在跨站图上会误导）；playbooks/analysis-discipline/figure-diagnostics 的"训练年2024"提法同步 | 用户："现在变成五站联合训练（都2025）预测也2025，测试站是雅砻江；五训练站=华能北方润达/国能和熙光储/泗洪/白马湖/西北戈壁小壕兔"（雅砻江不在训练里=留出） | 旧的"跨年分布外(2024vs2025)"整条主线作废，换成"跨站分布外"：核心问题变为"雅砻江离5训练站分布多远、最像哪个训练站"；漂移诊断基准从2024改为训练站集合、且必须逐站看(pooled会抹平"像小壕兔不像泗洪")；润达/和熙查不到按"不编造"标待补；合成数据验证三脚本+逐站PSI+train/test 重命名全通过 |
| 2026-07-09 | ①`plots.py` stats.json 补完整曲线+形状描述符（新增 `_curve_stats`；fig01 按功率分箱、fig04/07/09 逐日、fig05 全 192 步、fig06 全时段、fig11 补 p10/p90；顺带修 fig04/07 worst_days 的 date 键 json 报错、run_analysis fig02 用 normalize 保 DatetimeIndex 两处潜伏 bug）②三段 heredoc 转 `scripts/run_quality_check.py`/`run_analysis.py`（pred_col 自动侦测）/`run_drift.py`③详细模块挪进新 references：`analysis-discipline.md`（反驳门七条等）/`playbooks.md`（Playbook A/B + CONCLUSION）/`drift-and-nwp.md`④`SKILL.md` 485→201 行，只留骨架+精简纪律+指针⑤README/pv-analysis-resume/figure-diagnostics 指针同步 | 用户：stats.json 只存关键点、丢了整条曲线走势，走势结合数据才能定位模型弱点；SKILL.md 该 ~200 行、多余的挪 references | 走势图数据层从"压成单点"改为"整条曲线+描述符"，figure-diagnostics 形态判读从此可读 json 判（roughness 判毛刺、max_jump_idx 判台阶），不必肉眼看 PNG；SKILL 瘦身用"转脚本+挪 references"，强制纪律靠 SKILL 正文保留的"结论三道门"精简版+常见错误❌清单守住，full 清单在 analysis-discipline.md；合成数据验证曲线/描述符/三脚本全通过 |
| 2026-07-09 | `references/figure-diagnostics.md` 一次补齐 #1/#2/#4/#7/#8/#9/#11/#12 八节，覆盖进度表标全部已写（#3 来自 Excel、#10 待前提验证，标注按需补） | 用户："直接增加一下这几个板块吧" | 每节沿用「形态→含义→候选机制(挂现有 H-ID/模型)→怎么调→交叉验证」+ 图特有坑：#1 钉死 R²=corr²(齐不齐)vs slope(正不正)+ 四象限 + 限电封顶形态；#2 专答 ensemble 搞头(同质化/M2 低相关多样性源/某月骤降 H-M3-2)；#4 坏天共因 vs 特异(竖线只标首模型的坑)；#7 top-N 绝对 RMSE 被大晴天带偏(要看 ACC)；#8 月度变差因果分解(占比升 vs 同类退化)为核心+样本量坑；#9 oracle 不可实现是下界非 KPI+pick_share 路由；#11 特征/标签漂移+必用 weather_class_drift 共享基准；#12 组件衰减(整体下移)vs 限电(高段封顶)的形状区分。全部挂 H-CHRONOS-1/H-M1-1/H-M2-2/H-M4-1/H-LOSS-1/H-M3-1/H-M3-2 等现有 ID，不新造机制 |
| 2026-07-09 | `references/figure-diagnostics.md` 加 #5 误差随预报时效一节（8 形态 A–H + 时效指纹速查 + 动态加权洞察 + 两个坑）；`hypotheses.md` 新增 H-M2-6 | 用户与我讨论 #5，提出三个实测形态：开头台阶、结尾台阶暴涨、M2 高频毛刺；并强调"这些是可能情况，真实结果未必长这样，agent 要能检索相关知识去解释而非硬套" | #5 覆盖通用形态（单调升/平坦 M2/近端陡升/bias 时效漂移）+ 三个实测子案例各带**竞争解释的判别步骤**（开头台阶：惯性窗 vs 昼夜包络；结尾台阶：先排样本量伪影，再分远端原料塌 vs M3 padding；M2 毛刺：只有 M2 毛=架构特异 vs 全体毛=昼夜噪声，看周期 32/96 定位）；核心判别杠杆="共享效应全模型都有 vs 模型特异只某条"；节首加显式"这些是候选形态非清单，匹配不上就如实描述+从机制回推"红线；H-M2-6 把 M2 毛刺登记为可判别预测（H-M2-1 只说平坦，毛刺是新一层） |
| 2026-07-09 | 新建 `references/figure-diagnostics.md`（图谱形态判读库，先写 #6 一节）；`SKILL.md` references 表加一行、Playbook B 第 3/5 步接入 | 用户：#1/#6 这些"图形态→机制→怎么调"的分析很有用但模型有时想不到，放进库更有效，且能和模型架构联动；先写 #6，其余逐张补 | 建"现象→架构"反向索引，补上 stats.json 数字与 hypotheses/models 之间缺的一环，防漏诊；严格框成"候选假设非结论"（挂 H-ID、过反驳门），守住 Stage3/4 事实-故事隔离；#6 覆盖 8 种 bias/RMSE 形态 + 各模型指纹 + 三个读图坑，全部挂现有 H-LOSS-1/H-M2-x/H-WXSRC-1 等 ID |
| 2026-07-09 | 分布漂移诊断固化成可跑代码：`data_utils` 加 `clear_sky_envelope`/`daily_kt_sigma`（从 daily_weather_class 抽出，daily_weather_class 加 envelope/sig_hi 可选参数）、`drift_table`（特征+标签漂移逐变量逐月 PSI/KS 数值表）、`weather_class_drift`（天气型漂移，用 2024 共享基准）；SKILL.md Step 1 加载 train_set 并抽查、分布漂移诊断节加标准命令序列；scripts/README 函数索引更新 | 用户：分布漂移是重要后续建议，训练/测试都按 list 存（timestamp=序列起点），要 Step 1 也 load 训练集、并把这个 test 写进 script 让技能直接用 | 画图层(fig11/12)早已有，缺的是 Step1 载 train + 把 rebuild_series 对各变量接线的驱动函数；天气型跨年对比的命门是共享基准——各年自归一化会把"整体变暗/更多波动天"抹平，故 weather_class_drift 强制用 2024 包络+σΔ 阈值分类 2025；合成数据验证：变暗被 kt 中位数 0.78→0.48 捕捉、默认路径与 fig08/11/12 回归通过 |
| 2026-07-09 | `scripts/plots.py` 补齐 stats.json 自足性（#1 加 intercept/n/true_max/pred_max、#11 逐月分位数摘要 n/mean/std/p10/p25/median/p75/p90 双侧）+ 模块 docstring 改"PNG 只给人看、模型读 json"；`SKILL.md`(132/151)、`references/figure-diagnostics.md`(24/211)、`scripts/README.md`、`pv-analysis-resume/SKILL.md`(35/57/72-84/104) 全部把"Read PNG 确认形态/核心图必看 PNG"改为"一律读 stats.json、不 Read 图"；**撤销 07-09 上一条"核心图必 Read PNG"** | 用户：小上下文模型(264k)跑完 stats.json + 8 图后续不下去，prompt 已 240k；要求不让模型读图、每图 json 含图上全部数字 | 实测 stats.json 每张才 60–250 token、8 图像共 ~7k token，240k 是四阶段整条流水线累积；读图费上下文且非必要——stats.json 补到自足后"读 json ≡ 看图"，判形态的曲线/矩阵/分位数/描述符全在 json，遂全局改为不读图。上一条为"新会话没画过图、只读 json 漏形状证据"而要求读图，现由完整曲线+形状描述符+分位数承载形状证据，前提消失，予以撤销 |
| 2026-07-09 | `SKILL.md` 数据结构节 + Step1 schema 侦察加"true_label 只用 observe_power_future、其余训练特征列无关"警示；`scripts/data_utils.py` CONFIG 的 LABEL_COL/GHI_COL 注释同步 | 用户：true_label parquet 可能带完整训练特征列，只关注 observe_power_future，其余无用 | true_label 常沿用训练 schema，schema 侦察会把一堆特征列打印出来易被误当评估输入；scripts 取 label 本就走 LABEL_COL，但手写 pandas 时可能顺手混入气象列。跨站漂移的 test 侧气象列是否可用（雅砻江真实值 vs 训练遗留占位）留给用户确认，拿不准只报 label 指标 |
| 2026-07-10 | **git revert 前一条"改苏州"的 commit（85ea50d），恢复雅砻江=四川柯拉高原版**；station.md 测试站块补注：一度被"雅砻江在苏州"带偏、后经用户确认是乌龙，省份=四川【用户确认】、柯拉/4600m 具体仍【检索】 | 用户："不对不对，这是个乌龙，雅砻江就在四川" | 上一条 85ea50d 把测试站从四川高原改成江苏苏州、连带重写 H-XSTN-1/降级 H-M1-2/翻转迁移锚等，全部基于错误前提；用户确认雅砻江确在四川，故整条 revert 恢复高原版（比手工逐处回改更干净、无遗漏），仅在 station.md 留一句乌龙记录防再被带偏 |
| 2026-07-10 | **合并续跑技能 + 阶段日志 + subagent 编排**：新建 `scripts/run_orient.py`（Step 0 定位阶段 + 前置 ✓/✗ 检查 + `--goto N` 直达校验，对照真实产物核验 `analysis_state.json`、维护 `PROGRESS.md`）；新建 `references/subagent-briefs.md`（figure+fact / metric 两类子 agent 固化 prompt）；`SKILL.md` 加 Step 0 Orient 节 + 编排模型节 + description 并入续跑触发词，"不设状态文件"改为"经核验的产物清单"，运行后回顾加"每阶段更新 state/PROGRESS"；`pv-analysis-resume/SKILL.md` 缩成薄 stub 指回主技能；`scripts/README.md` 补 run_orient + state/log + 并行派发说明 | 用户：流程太长、Stage 1–3 生成/画图消耗主 agent token，能否用 subagent 并行生成、主 agent 只调度、画完直接做事实提取，跨结果归因留最后；且合并主/续跑技能、每阶段留日志好让 clear 上下文后能续跑、能直达 Stage X、进阶段前能查前置 | ①最大 token 收益=figure subagent 读 stats.json 返回现象、主 agent 全程不加载图像，且多 range 可并行；②Stage 3"只写事实禁机制"防火墙搬进 subagent brief，Stage 3→4 停顿点与 Stage 4 归因/反驳门留主 agent（subagent 不能问用户、不做跨结果综合）；③日志用"经核验的产物清单"——保留旧"真相以产物为准"哲学（orient 每次重扫真实文件、state 说谎就降级）同时满足"要显式 log 能续跑";④续跑并入主技能 Step 0，resume 缩 stub 保触发兼容；合成产物目录自测：判阶段/核验降级/--goto 守卫全通过 |
| 2026-07-13 | **删除 `pv-analysis-resume/` 目录**（stub 已无实际内容）；`README.md`/`结果分析Skill介绍.md` 同步去掉该技能的表格行、symlink 安装命令与"三技能"表述，改为主技能自带续跑，并补入 `pv-station-influence`；本 CHANGELOG 历史行原样保留 | 用户："如果合并就把它清理掉吧" | 07-10 已把续跑逻辑并入主技能 Step 0、resume 仅剩指回主技能的薄 stub，继续保留会让用户误以为是现役独立技能、README 安装命令还会 symlink 一个空壳；触发词早已并入主技能 description，删目录不丢功能。历史 CHANGELOG 提及 resume 的行属追溯记录，不改 |
| 2026-07-13 | **`pv-station-influence` 三项扩展**：①orient 检测/链接白马湖线 pv-result-analysis 产物（config 加 `result_analysis_workdir`/`result_analysis_status`，si_common 新增 detect_result_analysis 只读扫描 + station 校验；[absent] 时 orient 指引主 agent 先问用户、同意则在独立目录 `result_analysis_baimahu/` 嵌入跑主技能 Stage 1–3 复用 Brief A/B）；②新增 Stage 1 训练动力学（新建 `loss_dynamics.py`：final_loss/conv_slope ~ 站成员回归复用 influence_regression 实现 + rmse_link 同现检验 + `--from-ckpt`；probe_logs 改双探落 `probe_summary.json`；adapter 加可选 `load_loss_history`；原 Stage 1–4 顺移为 2–5，无 loss 记录时 Stage 1 跳过不阻塞）；③新建 `references/subagent-briefs.md`（嵌入式运行程序 + Brief C ckpt 分片/D TracIn/E 动力学/F 漂移），ckpt_eval/tracin 加 `--out`/`--raw` 分片防并发写竞态；influence-methods 加动力学节+loss×harm 四象限、attribution-discipline 加"loss 高≠有罪"小节、SKILL.md 93→~140 行同步 | 用户：本技能实为"pv-result-analysis + 训练侧（checkpoint）"，已跑过主技能就复用其分析并加训练 loss 归因（连接站点信息），没跑过要先问用户；工作量大需 subagent 等架构 | 两技能是并行实验线，嵌入运行独立目录+station 校验防污染雅砻江；动力学放 Stage 1（只依赖 Stage 0、观测性零 GPU、为反驳门#3 备旁证），"loss 高≠有罪"与"震荡≠有罪"对偶成文（loss 低×harm 高=分布冲突指纹）；M1–M4 损失量纲不可比故逐模型独立只比 Spearman；单写者纪律沿用主技能（state/PROGRESS/FINDINGS/config 只主 agent 写）。合成产物自测 27 断言全过（动力学埋坏站排第一/orient 三分支+并行线保护/probe 双探不误报/分片合并等价） |
| 2026-07-13 | 模型参考改为经 model-ref.pointer 定位 `.modelmap/models.md`；删除口述 models.md；配套技能改名 pv-model-analysis（生成式） | 用户要求：模型档案由代码生成、供人+供分析消费，分析时定位或触发生成 | 口述档案会漂移；代码锚定 + 置信标签让归因前提可靠、可增量核验 |
